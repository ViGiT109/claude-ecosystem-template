<!--
Claude Code-specific additions appended to CLAUDE.md by scripts/sync_agents_md.py
after the contents of AGENTS.md.

Use this file for instructions that only make sense for Claude Code (e.g. references
to Claude-specific features that other agent tools do not implement). Keep the
universal rules in AGENTS.md.

When this file is empty, CLAUDE.md == AGENTS.md (plus the auto-generated header).
-->
